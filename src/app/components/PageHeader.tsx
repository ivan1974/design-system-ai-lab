import { InstalledBaseHeader } from '../../design-system/components';

export function PageHeader() {
  return (
    <InstalledBaseHeader
      siteName="Demo Data Center — DC-WE01"
      contextLabel="Reference Campus"
      buildingCount="2"
      roomCount="3"
      statusLabel="Component hierarchy + intelligence enabled"
      portfolioTitle="Asset Portfolio"
    />
  );
}
