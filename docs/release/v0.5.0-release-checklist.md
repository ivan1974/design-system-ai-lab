# v0.5.0 Release Checklist

## Release name

```txt
0.5.0 — Controlled Generation Standard
```

## Purpose

Use this checklist before publishing the v0.5.0 package.

This file is operational release documentation.

It does not replace:

```txt
guidelines/Guidelines.md
guidelines/setup.md
guidelines/make-minimal-contract.md
contracts/
```

---

## Required checks

- [ ] `npm install` has been run after the version bump.
- [ ] `package.json` version is `0.5.0`.
- [ ] `package-lock.json` version is `0.5.0`.
- [ ] `npm run check` passes.
- [ ] `npm run build:ds` passes.
- [ ] `npm run build-storybook` passes.
- [ ] `npm pack --dry-run` passes.
- [ ] Package contents include `dist/`.
- [ ] Package contents include `README.md`.
- [ ] Package contents include `CHANGELOG.md`.
- [ ] Package contents include `contracts/`.
- [ ] Package contents include `guidelines/make-minimal-contract.md`.
- [ ] Package contents include `guidelines/Guidelines.md`.
- [ ] Package contents include `guidelines/setup.md`.
- [ ] Package contents include `guidelines/migration/`.

---

## Generation governance checks

- [ ] Contracts are valid JSON.
- [ ] Golden examples pass generation tests.
- [ ] Benchmark cases are documented.
- [ ] Scoring model is documented.
- [ ] Repair routing is documented.
- [ ] Make Minimal Contract is documented.
- [ ] README mentions the Controlled Generation Standard.
- [ ] CHANGELOG includes `0.5.0`.
- [ ] Migration guide explains v0.4 to v0.5 changes.

---

## Commands

Run:

```bash
npm install
npm run check
npm run build-storybook
npm pack --dry-run
```

Optional real Make validation:

```bash
GENERATED_APP_PATH=benchmarks/figma-make/outputs/01-customer-monitoring.App.tsx npm run test:generation
```

---

## Release notes summary

Use this summary in the release announcement:

```txt
v0.5.0 introduces the Controlled Generation Standard.

The design system now includes machine-readable generation contracts, stricter critical props, tested golden examples, Figma Make benchmark cases, a scoring model, repair routing and a Make Minimal Contract.
```

---

## Final decision

- [ ] Publish approved.
- [ ] Publish blocked.

Reason:

```txt

```
