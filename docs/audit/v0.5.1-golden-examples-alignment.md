# v0.5.1 Sprint 9 — Golden Examples Alignment

## Purpose

Sprint 9 aligns golden examples so they become canonical GenAI references, not just demos.

Goal:

```txt
Every golden example demonstrates current v0.5.1 guidance.
```

---

## Scope

Golden examples:

```txt
guidelines/examples/golden/customer-monitoring.App.tsx
guidelines/examples/golden/renewal-risk-review.App.tsx
guidelines/examples/golden/asset-recommendation-review.App.tsx
guidelines/examples/golden/qbr-readiness.App.tsx
guidelines/examples/golden/installed-base-explorer.App.tsx
```

---

## Alignment performed

### Public package imports

Golden examples continue to import from:

```txt
design-system-ai-lab
design-system-ai-lab/styles.css
```

The test now blocks relative or internal imports.

---

### Current workspace architecture

Golden examples now consistently demonstrate:

```txt
WorkspaceShell
PageHeading
MasterDetailLayout
WorkspaceDetailPanel
ListContainer
```

They avoid legacy page/detail patterns.

---

### Business pattern usage

Golden examples demonstrate current business patterns instead of rebuilding sections manually:

```txt
CustomerStatusCard
CustomerReviewReadinessCard
ConnectivityCoverageCard
AssetIntelligenceSummary
RecommendationReviewPanel
RenewalRiskSummary
ServiceRiskSummary
ValueProofCard
Queue row patterns
```

---

### Queue row density

Golden examples use queue rows inside `ListContainer` for repeated business objects:

```txt
AssetQueueRow
RiskQueueRow
RecommendationQueueRow
```

This reinforces the v0.5.1 rule:

```txt
Repeated business objects should not default to card stacks.
```

---

### Evidence and trust behavior

Updated examples to use canonical trust and readiness values:

```txt
sourceStrength: partial | validated
validationStatus: internal-review-needed
customerReadiness: needs-review
proofReadiness: expected-only | internal-proof
readiness: needs-review | internal
status: todo | in-progress
```

Deprecated or vague snippets were removed from the golden examples.

---

### Actionability

Golden examples keep follow-through visible through:

```txt
ActionRow
StickyActionBar
Button actions attached to package components
```

Actions retain owner, due date and priority.

---

## Files updated

```txt
guidelines/examples/golden/customer-monitoring.App.tsx
guidelines/examples/golden/installed-base-explorer.App.tsx
guidelines/examples/golden/renewal-risk-review.App.tsx
guidelines/examples/golden/qbr-readiness.App.tsx
guidelines/examples/golden/asset-recommendation-review.App.tsx
```

---

## Test updates

Updated:

```txt
tests/generation-rules/golden-examples.test.ts
```

The test now verifies:

```txt
fixture list remains canonical
all examples export default App
all examples render visible main screens
imports come from the public package root
current workspace architecture is present
trust, validation and actionability are visible
obsolete or deprecated snippets are absent
```

Blocked snippets include:

```txt
sourceStrength="strong"
readiness="needs_review"
status="in_progress"
PageHeader
DetailPanel
CompanyName
```

---

## Acceptance status

```txt
Passed for Sprint 9 scope, pending local lint, test and build validation.
```

Acceptance criteria:

```txt
Every golden example is contract-compliant.
Every golden example demonstrates current v0.5.1 guidance.
No golden example teaches obsolete patterns.
```

---

## Local validation commands

```bash
npm run lint:md
npm run test
npm run build:ds
npm run build-storybook
```
