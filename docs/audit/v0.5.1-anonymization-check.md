# v0.5.1 Anonymization Check

## Purpose

This audit records the anonymization pass before continuing the `v0.5.1` documentation hardening roadmap.

Goal:

```txt
Remove explicit or indirect proprietary references from public package documentation, stories, contracts, tests and benchmarks.
```

---

## Checked terms

Direct terms checked:

```txt
EcoCare
Schneider
Schneider Electric
EcoStruxure
mySchneider
```

Indirect or contextual terms checked:

```txt
Connected Services Hub
CSH
CSM
CompanyName Advanced Plus
```

---

## Search result

Direct search for the main proprietary names returned no indexed results.

A manual file check found indirect references in:

```txt
src/design-system/stories/v0.5.1-critical-generation.stories.tsx
```

The story contained anonymization risks such as role acronyms and proprietary-context wording.

---

## Changes made

Updated:

```txt
src/design-system/stories/v0.5.1-critical-generation.stories.tsx
```

Replacements:

```txt
CSM → Account owner
CSH → operations review
Connected Services Hub → Operations hub
Advisor → Monitoring platform
CompanyName Advanced Plus → Premium service plan
```

The story now uses generic B2B service language.

---

## Guard added

New test:

```txt
tests/generation-rules/anonymization.test.ts
```

The test scans:

```txt
.md
.mdx
.ts
.tsx
.json
```

It ignores build and dependency folders:

```txt
.git
node_modules
dist
storybook-static
```

It also excludes its own test file so the forbidden-term list does not fail the test.

---

## Acceptance status

```txt
Passed for repository-level anonymization guard, pending local test validation.
```

Run:

```bash
npm run test
npm run lint:md
```

If the test fails, every finding should be either anonymized or explicitly excluded with a documented rationale.
