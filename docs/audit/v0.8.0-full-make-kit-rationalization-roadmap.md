# v0.8.0 Full Make Kit Rationalization Roadmap

## Status

Target roadmap for v0.8.0.

v0.8.0 is not a screen-specific refactor. It is a full Make Kit rationalization and visual alignment release.

Installed Base Intelligence remains the strict contractual screen. Customer Monitoring becomes the second generation proof. The entire active design system surface used by Make-facing examples must be rationalized, restyled, aligned and tested.

## Strategic goal

Move from v0.7.0-alpha.0 Installed Base screen-contract proof to a smaller, stricter and more visually consistent Make Kit that can generate multiple product screens without local components, visual drift or component proliferation.

The prototype provided for v0.8 defines the visual quality bar for the whole design system, not only for Installed Base.

## Non-negotiable principles

1. The prototype defines the visual grammar for the whole active DS surface.
2. Installed Base is the contractual screen, but the DS rationalization is global.
3. Customer Monitoring must be generable with the same visual quality and component grammar.
4. Brand references from the prototype must be anonymized, but the visual grammar remains the reference.
5. No component can remain public only because it already exists.
6. Same visual structure means same component.
7. Different business content does not justify a new component.
8. Business meaning is controlled by contracts, props and allowed values, not by one component per label.
9. Components used by active examples must be audited, rationalized, documented, contracted, storied and tested.
10. Application-specific logic must not remain in the design system surface.
11. Legacy artifacts must be deleted or moved outside the active Make surface.

## Target capability

After v0.8.0, one prompt must be able to generate:

- Installed Base Intelligence, with strict screen-contract compliance.
- Customer Monitoring, with the same visual system, density, token usage and rationalized component grammar.

The refactor must not degrade functionality or interaction capability. It should preserve or improve:

- filtering;
- table/list density;
- panel interactions;
- progressive decision disclosure;
- evidence and trust display;
- actionability;
- screen-level context preservation;
- benchmark repairability.

## Scope

The v0.8 rationalization covers all active Make-facing surfaces:

```txt
src/design-system/components/
src/design-system/composition/
src/design-system/decision/
src/design-system/forms/
src/design-system/patterns/
src/design-system/stories/
guidelines/examples/golden/
benchmarks/figma-make/cases/
benchmarks/figma-make/scoring/
guidelines/runtime/
guidelines/reference/
guidelines/evaluation/
contracts/
```

No component used by active examples is out of scope.

Active examples to protect and align:

```txt
guidelines/examples/golden/customer-monitoring.App.tsx
guidelines/examples/golden/installed-base-intelligence.App.tsx
guidelines/examples/golden/renewal-risk-review.App.tsx
guidelines/examples/golden/asset-recommendation-review.App.tsx
```

Installed Base and Customer Monitoring are the mandatory generation proofs for v0.8.0.

## Core architectural decision

The screen contract controls meaning. The design system controls visual grammar.

Do not keep one public component per business label when the rendering structure is identical.

Preferred v0.8 semantic primitives:

```txt
SemanticTag
SemanticPill
StatusIndicator
MetaLabel
```

Specialized components should be deleted, merged or internalized unless a distinct structure is justified:

```txt
HealthPill
CoverageTag
DppTag
ConnectivityLabel
StatusWithIcon
PriorityLabel
ProofStatus
SourceStrength
```

The same principle applies to cards and decision components.

## Card rationalization target

All cards across components, decision and patterns must be audited.

A card is not a component type because its content is different. A card becomes a separate component only if its visual structure, interaction behavior or reusable composition responsibility is different.

Target grammar:

```txt
Card
MetricBlock
DecisionBlock
EvidenceBlock
ActionBlock
StatusSummary
```

Likely merges:

```txt
AlertCard -> Card or DecisionBlock
RecommendationCard -> DecisionBlock
ActionCard -> ActionBlock
EvidenceCard -> EvidenceBlock
InsightCard -> DecisionBlock or Card
MetricCard variants -> MetricBlock
RiskCard -> DecisionBlock or StatusSummary
```

The final merge list must be based on the real inventory audit.

## DS vs application boundary

The DS owns:

- visual primitives;
- semantic display primitives;
- reusable decision structures;
- reusable composition patterns;
- screen contracts;
- design tokens;
- allowed values and mappings;
- generation rules;
- repair rules;
- benchmark expectations.

Applications own:

- data fetching;
- backend filtering;
- routing;
- authorization;
- persistence;
- real customer data;
- real asset lifecycle logic;
- analytics events;
- application-specific flows.

If a component only encodes one app's data or screen copy, it is not a DS component.

## Alignment rule

No component, token, pattern or screen rule is valid until it is:

- documented in guidelines;
- represented in a contract when relevant;
- implemented in code;
- exported if public;
- covered by Storybook;
- covered by tests;
- reflected in golden examples if used;
- reflected in prompts if used by Make;
- reflected in benchmarks and repair prompts when relevant.

## Sprint 1 — Full active surface inventory

Objective: audit the entire active design system surface, not only Installed Base.

Create:

```txt
docs/audit/v0.8.0-full-component-inventory.md
docs/audit/v0.8.0-example-usage-map.md
docs/audit/v0.8.0-card-rationalization-audit.md
docs/audit/v0.8.0-decision-components-audit.md
docs/audit/v0.8.0-patterns-audit.md
docs/audit/v0.8.0-alignment-gap-analysis.md
```

Each component must be classified as:

```txt
keep
merge
rename
internalize
delete
app-specific
```

Exit criteria:

- every component used by active examples is inventoried;
- every public export is mapped to usage;
- every card-like component is compared structurally;
- every decision component is assessed for DS value;
- every pattern is classified as reusable DS or app-specific.

## Sprint 2 — Prototype visual system extraction

Objective: convert the shared prototype's Tailwind/shadcn-compatible style into global DS tokens and rules.

Update or create:

```txt
src/design-system/tokens.css
src/design-system/styles.css
guidelines/tokens.md
guidelines/styles.md
guidelines/runtime/visual-rules.md
guidelines/runtime/no-local-style-generation.md
contracts/tokens.contract.json
contracts/visual-rules.contract.json
tests/generation-rules/tokens.test.ts
tests/generation-rules/no-hardcoded-visual-values.test.ts
tests/generation-rules/no-inline-style-drift.test.ts
```

The prototype must define:

- white-first surfaces;
- anonymized brand green usage;
- neutral borders;
- typography scale;
- dense rows;
- compact controls;
- table-first alignment;
- subtle dividers;
- panel width and overlay behavior;
- card density;
- tag and pill styling;
- button styling.

Exit criteria:

- tokens replace hardcoded visual values;
- all active components can use the same visual grammar;
- no part of the active DS keeps a separate visual style.

## Sprint 3 — Semantic display primitive rationalization

Objective: unify tags, pills, labels, badges and status displays.

Create or stabilize:

```txt
SemanticTag
SemanticPill
StatusIndicator
MetaLabel
```

Update:

```txt
contracts/semantic-display.contract.json
contracts/props.contract.json
contracts/component-registry.contract.json
contracts/package.contract.json
src/design-system/index.ts
src/design-system/stories/components/
tests/generation-rules/semantic-display-primitives.test.ts
tests/generation-rules/no-redundant-semantic-components.test.ts
tests/generation-rules/package-surface.test.ts
```

Exit criteria:

- business-specific tag, pill and status variants are merged or internalized;
- semantic labels, tones and icons are controlled by contracts;
- no generated output needs local badge, pill, tag or status components.

## Sprint 4 — Card and decision component rationalization

Objective: rationalize all card-like and decision components across components, decision and patterns.

Create or stabilize:

```txt
Card
MetricBlock
DecisionBlock
EvidenceBlock
ActionBlock
StatusSummary
```

Update the matching component guidelines, props contract, component registry, stories and tests.

Exit criteria:

- all active cards use the same tokenized visual grammar;
- redundant card components are deleted, merged or internalized;
- decision components represent reusable decision structures, not business-specific wrappers;
- active examples do not look like separate mini design systems.

## Sprint 5 — Pattern and application-boundary rationalization

Objective: ensure patterns are reusable Make Kit compositions, not application logic.

Audit and refactor:

```txt
src/design-system/patterns/customer-monitoring/
src/design-system/patterns/installed-base/
src/design-system/patterns/*
```

Create or update:

```txt
guidelines/reference/architecture/ds-vs-application-boundary.md
contracts/package-boundary.contract.json
tests/generation-rules/ds-vs-app-boundary.test.ts
```

Exit criteria:

- patterns contain reusable screen composition only;
- application-specific mock logic is moved to examples;
- data-fetching, persistence and backend filtering are not encoded in DS components.

## Sprint 6 — Refactor all active golden examples

Objective: make all active examples use the rationalized v0.8 surface and shared visual grammar.

Refactor:

```txt
guidelines/examples/golden/customer-monitoring.App.tsx
guidelines/examples/golden/installed-base-intelligence.App.tsx
guidelines/examples/golden/renewal-risk-review.App.tsx
guidelines/examples/golden/asset-recommendation-review.App.tsx
```

Rules:

- use only public DS exports;
- use no local components;
- use no local visual styles;
- use semantic primitives instead of business-specific display variants;
- use rationalized card and decision blocks;
- preserve existing functionality and interactions.

Exit criteria:

- Installed Base and Customer Monitoring share the same visual system;
- Renewal Risk and Asset Recommendation do not introduce legacy visual grammar;
- all golden examples compile and remain functionally equivalent or better.

## Sprint 7 — Installed Base contract update

Objective: keep Installed Base strict while aligning it with the rationalized surface.

Update:

```txt
guidelines/reference/screen-contracts/installed-base-intelligence.md
contracts/screen-contracts/installed-base-intelligence.contract.json
contracts/installed-base-domain.contract.json
contracts/semantic-display.contract.json
tests/generation-rules/installed-base-screen-contract.test.ts
tests/generation-rules/installed-base-semantic-display.test.ts
benchmarks/figma-make/cases/12-installed-base-intelligence-closed-spec.md
benchmarks/figma-make/scoring/installed-base-intelligence-scoring.md
```

Exit criteria:

- strict layers, filters, columns, tabs, actions and states remain preserved;
- the contract references semantic primitives and controlled mappings instead of redundant business-specific visual components;
- no functionality is degraded.

## Sprint 8 — Customer Monitoring generation contract

Objective: make Customer Monitoring a second v0.8 generation proof.

Create or strengthen:

```txt
guidelines/reference/screen-contracts/customer-monitoring.md
contracts/screen-contracts/customer-monitoring.contract.json
guidelines/prompts/customer-monitoring.md
benchmarks/figma-make/cases/customer-monitoring-v08.md
benchmarks/figma-make/scoring/customer-monitoring-scoring.md
tests/generation-rules/customer-monitoring-screen-contract.test.ts
tests/generation-rules/customer-monitoring-golden.test.ts
tests/generation-rules/customer-monitoring-prompt.test.ts
```

Exit criteria:

- one prompt can generate Customer Monitoring with the same v0.8 style;
- Customer Monitoring uses the rationalized component grammar;
- Customer Monitoring does not regress into a generic dashboard or local card system.

## Sprint 9 — Storybook alignment

Objective: make Storybook the visible proof of the rationalized DS.

Required story groups:

```txt
Foundation / Tokens
Foundation / Typography
Foundation / Density
Components / SemanticTag
Components / SemanticPill
Components / StatusIndicator
Components / MetaLabel
Components / Card
Decision / MetricBlock
Decision / DecisionBlock
Decision / EvidenceBlock
Decision / ActionBlock
Decision / StatusSummary
Screens / Installed Base Intelligence
Screens / Customer Monitoring
```

Exit criteria:

- every public component has a story;
- stories use public exports;
- screen stories prove visual consistency across Installed Base and Customer Monitoring.

## Sprint 10 — Documentation, contracts, prompts and repair alignment

Objective: align every Make-facing artifact.

Update:

```txt
README.md
CHANGELOG.md
guidelines/Guidelines.md
guidelines/setup.md
guidelines/runtime/component-selection.md
guidelines/runtime/generation-flow.md
guidelines/runtime/screen-contract-generation.md
guidelines/runtime/visual-rules.md
guidelines/reference/architecture/component-taxonomy.md
guidelines/reference/architecture/public-surface.md
guidelines/evaluation/repair/repair-router.md
src/design-system/ai/prompt-library.md
src/design-system/ai/figma-make-setup.md
src/design-system/ai/make-kit-guidelines.md
```

Create repair prompts for new failure modes:

```txt
guidelines/evaluation/repair/redundant-semantic-component.md
guidelines/evaluation/repair/card-proliferation.md
guidelines/evaluation/repair/uncontracted-semantic-tone.md
guidelines/evaluation/repair/hardcoded-visual-style.md
guidelines/evaluation/repair/token-drift.md
guidelines/evaluation/repair/customer-monitoring-dashboard-drift.md
```

Exit criteria:

- docs, contracts, code, prompts, repair prompts and benchmarks describe the same v0.8 surface;
- no guideline references deleted components;
- no benchmark rewards deprecated components.

## Sprint 11 — Benchmark and adversarial suite update

Objective: make benchmark scoring enforce the new rationalized surface.

Update existing Installed Base cases and add:

```txt
benchmarks/figma-make/cases/18-adversarial-too-many-semantic-components.md
benchmarks/figma-make/cases/19-adversarial-card-proliferation.md
benchmarks/figma-make/cases/20-adversarial-hardcoded-visual-styles.md
benchmarks/figma-make/cases/21-adversarial-customer-monitoring-dashboard-drift.md
```

Benchmark failures must catch:

- local components;
- too many semantic components;
- redundant card variants;
- hardcoded visual values;
- generic dashboard drift;
- unsupported tones;
- deprecated component usage;
- loss of Installed Base contract functionality;
- loss of Customer Monitoring functionality.

Exit criteria:

- benchmarks validate both visual consistency and functional preservation;
- adversarial prompts fail when they reintroduce component proliferation or legacy styling.

## Sprint 12 — Alignment tests and release gate

Objective: make alignment automatic.

Create or reinforce:

```txt
tests/generation-rules/documentation-contract-alignment.test.ts
tests/generation-rules/component-registry-alignment.test.ts
tests/generation-rules/props-contract-alignment.test.ts
tests/generation-rules/package-surface.test.ts
tests/generation-rules/story-coverage.test.ts
tests/generation-rules/golden-storybook-alignment.test.ts
tests/generation-rules/benchmark-contract-alignment.test.ts
tests/generation-rules/prompt-system-alignment.test.ts
tests/generation-rules/repair-router-alignment.test.ts
tests/generation-rules/public-surface-minimality.test.ts
```

Exit criteria:

- release fails if a public component lacks docs, props contract, registry entry, story or test;
- release fails if golden examples import deprecated or internal components;
- release fails if benchmarks and contracts diverge.

## Sprint 13 — Legacy deletion and v0.8 release preparation

Objective: remove anything not aligned with v0.8.

Delete or move outside the active Make surface:

```txt
deprecated components
obsolete decision cards
obsolete pattern wrappers
old stories
old guidelines
old props contract entries
old registry entries
unused exports
examples not aligned with v0.8
```

Release commands:

```bash
npm run check
npm run build
npm run build:ds
npm run build-storybook
npm run test:generation
npm pack --dry-run
```

Exit criteria:

- no legacy artifact remains in the active Make surface;
- package surface is smaller and clearer;
- v0.8.0 can be published only if Installed Base and Customer Monitoring generation proofs pass.

## Global exit criteria

v0.8.0 succeeds only if:

1. all components used by active examples have been audited;
2. all cards have been rationalized;
3. all decision components have been rationalized;
4. all active patterns have been rationalized;
5. visually identical components have been merged;
6. business role differences are handled by content, props, mappings or contracts;
7. the prototype visual system is applied to the whole DS;
8. tokens replace hardcoded styles across the active surface;
9. Installed Base remains the strict contractual screen;
10. Customer Monitoring becomes the second generation proof;
11. active golden examples share the same visual grammar;
12. prompts exist for Installed Base and Customer Monitoring;
13. prompts generate without local components;
14. benchmarks penalize component proliferation;
15. adversarial prompts cover redundant cards and semantic variants;
16. app-specific components are deleted, internalized or moved out of DS;
17. legacy components are deleted;
18. guidelines align with code;
19. contracts align with code;
20. stories align with public exports;
21. tests cover the public surface;
22. benchmarks reflect the v0.8 strategy;
23. repair prompts reflect v0.8 failure modes;
24. README and CHANGELOG reflect the v0.8 surface;
25. `npm run check` passes;
26. `npm run build-storybook` passes;
27. `npm pack --dry-run` shows a clean package surface.

## Final formulation

v0.8.0 is a full Make Kit rationalization release.

Installed Base remains the strict contractual screen.

Customer Monitoring becomes the second generation proof.

The entire active design system surface is restyled, simplified, aligned and tested.
