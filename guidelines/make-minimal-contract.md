# Make Minimal Contract

## Status

```txt
LEGACY COMPATIBILITY REDIRECT
```

This file is kept only for compatibility with older v0.5.x documentation and prompts.

It is no longer the active Figma Make runtime contract.

The active v0.6.0 runtime contract is:

```txt
guidelines/runtime/generation-contract.md
```

---

## Required read path

For v0.6.0, use:

```txt
guidelines/Guidelines.md
guidelines/setup.md
guidelines/tokens.md
guidelines/styles.md
guidelines/runtime/generation-contract.md
guidelines/runtime/generation-flow.md
guidelines/runtime/component-selection.md
guidelines/runtime/trust-action-rules.md
guidelines/runtime/visual-rules.md
guidelines/runtime/progressive-decision-disclosure.md
```

---

## Compatibility rule

If this file conflicts with `guidelines/runtime/generation-contract.md`, the runtime contract wins.

Do not add new rules here.

Do not ask Figma Make to read this file by default for v0.6.0.
