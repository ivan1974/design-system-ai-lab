# Repair prompt — missing ListContainer

Use this prompt when a generated screen shows repeated rows without the package list container.

## Instruction

Use `ListContainer` for grouped rows.

Use queue row components for repeated customers, assets, risks and recommendations.

Do not create a local list shell.

## Acceptance criteria

The repaired screen uses `ListContainer`, package row components and no local list styling.
